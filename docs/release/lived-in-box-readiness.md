# Lived-in Box readiness

Status date: 2026-08-17 (Australia/Melbourne)

This ledger separates source completion from release acceptance for the [Lived-in Box feature](../features/lived-in-box.md). It is not a candidate manifest and does not authorize shipping. Update the relevant row in the same change as the work it describes; never claim an evidence level the artifacts do not support (simulator evidence is not device evidence; unsigned builds are not packaged evidence).

| Milestone | Source status | Evidence available now | Evidence still required |
| --- | --- | --- | --- |
| B0 — Contract freeze | **Accepted** (2026-08-16) | Feature specification, ADR 0004, development plan, this ledger, baseline/README synchronization; the full decision ledger LB-D01…D20 is owner-approved as of 2026-08-16 — LB-D01/D02/D07 approved as specified, LB-D19 sole-3D-surface, LB-D04/D05/D06 future-roadmap deferrals, and the spec §4.4 pre-release persistence posture | — B0 exit is complete; B1 may begin per the development plan |
| B1 — Tangible core | In progress (WP-01…WP-03 and WP-06 complete; WP-04 owner review open) | The 3D scene is the only Home surface, with its visible controls and the MVP suites green in the simulator (`make check`); the `box-scene-v1` derivation core is fixture-tested and inside its reducer budget; scene module builds for arm64 device; capture now runs through the targeted lid with persisted-first success and failure hold | All B1 acceptance rows (SCN/LID/PULL/DIAL/PEEK/MOT/SND/FST/DGR/PRF/AXS), WP-04 owner review and device visual review, MVP + Share ingestion regression at Q0 and forced Q2, Instruments runs on both reference devices with the oldest-device tier gate met, quality-tier behavior matrix, gesture-equivalence audit, owner experience sign-off |
| B2 — States and traces | Not started | — | PAPR and B2 TRC rows, deletion/restore re-derivation matrices, device pass for completion and import sequences, bilingual copy review, owner sign-off |
| B3 — Growth and echoes | Not started | — | Remaining TRC rows, night selection regression, anniversary and discovery once-only proofs, final accessibility and performance re-runs, release-manifest integration, owner sign-off |

## Work-package status

| WP | Phase | Status | Evidence |
| --- | --- | --- | --- |
| WP-01 — Rendering spike and scaffolding | B1 | **Complete** (2026-08-16) | Source: `Features/Home/Scene/` module behind the Debug-only `SOMEDAYBOX_SCENE_SPIKE` flag, zero shipped behaviour change. Static: `make audit` (now including `scripts/audit-scene-layering.sh`) green. Simulator: `make ci-check` green on iPhone 17 Pro / iOS 26.5 — 52 unit tests and 5 UI tests, no regression against the pre-scene baseline. Build: `xcodebuild build -destination 'generic/platform=iOS'` succeeds for arm64 device (unsigned). Rendering: the placeholder box, lid, and ground render in the simulator, confirmed through a temporary, uncommitted harness and reverted; screenshot held outside the repository. API findings and the answered camera/backdrop/shadow questions are in development plan §6. **Not produced:** physical-device rendering, frame-rate/thermal/memory measurements, signed package — none are WP-01's scope |
| WP-02 — `box-scene-v1` derivation core | B1 | **Complete** (2026-08-16) | `BoxSceneState`, `BoxSceneStateReducer`, `DrawDialMapping`, `TimeOfDayLightDriver` — Foundation only, no renderer import. 36 new fixtures in `BoxSceneDerivationTests` and `DrawDialMappingTests` cover §6.2 bands at 0/1/12/13/48/49/200/201, `draw-dial-v1` snap at 10/29/30/45/90/119/120/300/480, §8 tiers at the exact 7/90/180-day boundaries with the long-kept quiet rule and future-date clamp, seeded-layout stability across relaunch and record order, a PAPR-01 proof that title and note changes cannot move any derived value, §6.4 gate precedence, and every §9.1 band boundary. `make ci-check` green: 88 unit tests, 5 UI tests, no MVP regression. **PRF-02 (reducer half):** 13.9 ms full pass over a dataset shaped like baseline §12.4 `performance-v1` (5,000 items / 25,000 attempts / 5,000 memories), against a 50 ms budget — measured in a Debug `-Onone` simulator build, so it is an upper bound, not device evidence. **Not produced:** the frozen digest-identified `performance-v1` artifact (does not exist in the repository), Release or device timing (WP-13) |
| WP-03 — `RealityView` host, camera rig, environment | B1 | **Complete** (2026-08-16) | The scene is Home: `BoxSceneStage` builds the environment and camera rigs behind `RealityView`'s async placeholder, the §9.1 rig drives the lights, and the graded backdrop is a SwiftUI layer beneath the scene (iOS gives `RealityView` no background of its own). The WP-01 spike layer and its `SOMEDAYBOX_SCENE_SPIKE` flag are retired; the legacy 2D Home layout is deleted rather than kept as scaffolding (plan §2 WP-03 correction, LB-D19). Ambient quiet cancels its refresh loop after 10 s idle and on leaving the foreground, so no timer runs while the app is away. Scene construction failure is contained at the boundary and shows a retry placeholder that WP-11 completes. `make ci-check` green: 93 unit tests (5 new stage fixtures), 6 UI tests including a new FST-01 test that the overlay controls are hittable and open the capture sheet while the stage is still building, and the automated accessibility audit now running against the scene Home. **Not produced:** measured 400 ms / first-frame timings (WP-13), device rendering, in-scene ambient animation to pause (nothing moves until WP-04), the §14.1 scene summary element (WP-12) |
| WP-04 — Procedural box, materials, paper stack ⚑ | B1 | Source complete; **owner art-direction review open** | `BoxGeometry` builds a real container — floor and four walls, a lid on its rear hinge, the draw strap, and the memory seam, letter slot, and bottom seam present but hidden until their predicates hold. `BoxMaterials` holds the §15.4 families in one place. `PaperStackLayer` renders the snapshot's instances from one shared mesh, compressing the stack so a full box still closes. `make ci-check` green: 102 unit tests (9 new rendering fixtures) and 6 UI tests. Simulator evidence: the closed idle box in light and dark, and the open box at 8 / 60 / 400 drawable papers (8 / 22 / 32 instances) captured through a temporary, reverted harness. **Not produced:** the ⚑ owner review of look and warmth, which gates B1 progress; device material truth; forms and age expression (WP-15) |
| WP-05 — Time dial and Custom minutes | B1 | Not started | — |
| WP-06 — Capture through the lid | B1 | **Complete** (2026-08-17) | `AnimationTierEngine` and `LidCaptureCoordinator` keep presentation history outside product truth, route the visible control and targeted lid gestures into one capture sheet, and run open/rise then persisted-success fold/drop/close or failure hover-and-hold. The first three, 30-second repeat, forced-minimal, timing-budget, collision-target, failure-hold, and persisted-success paths have nine focused fixtures. `make audit`, `make test`, and `make check` are green: 111 XCTest unit tests, 9 Swift Testing policy tests, and 9 UI tests with zero failures; the unsigned generic iOS arm64 build also succeeds. Simulator UI evidence covers the targeted rendered-lid hit region, focused long-press entry, forced commit-failure draft retention, and the Home/Settings accessibility audit. **Not produced:** physical-device material/shadow evidence, which remains a later B1 phase gate. LID-05 remains explicitly assigned to WP-17. |
| WP-07…WP-14 | B1 | Not started | — |
| WP-15…WP-20 | B2 | Not started | — |
| WP-21…WP-24 | B3 | Not started | — |

## Standing notes

- Share to Box S6 packaged acceptance remains tracked separately in [share-to-box-readiness.md](share-to-box-readiness.md); neither ledger substitutes for the other.
- The feature as specified needs no schema, backup, or policy change. The project is pre-release, so persistence may evolve when genuinely needed — but only docs-first with versioned identifiers (plan §0.2), never silently inside a work package.
- **Toolchain (2026-08-16):** the development machine's missing iOS platform component was installed the same day, restoring simulator and device destinations; `make ci-check` runs again. Simulator evidence in this ledger is still not device evidence — RealityKit material, frame-rate, thermal, and haptic truth stay open until the WP-13 device runs.
- By owner decision (LB-D19, 2026-08-16) there is no 2D product mode: degradation evidence concerns the in-scene quality-tier ladder and the data-safety recovery surface only. No work package may introduce a parallel Home surface.
